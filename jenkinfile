pipeline {
    agent any

    triggers {
        pollSCM('*/5 * * * 1-5')
    }
    
    stages {
        stage('Run') {
            steps {
                sh ''' python add.py
            }
        }
      }
  }
