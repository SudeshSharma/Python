pipeline {
    agent any

    triggers {
        pollSCM('*/5 * * * 1-5')
    }
    
    job('Python Program Running') {        
            steps {
               python {
                command('python add.py')
               }
           }
       }
   }
