pipeline {
    agent any

    triggers {
        pollSCM('*/5 * * * 1-5')
    }
    
    stages {
        stage('Run') {
            steps {
               python{
                command('python add.py')
               }
            }
        }
      }
  }
